<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Ergonomics Consulting LLC — Proactive Risk Reduction</title>
  <meta name="description" content="Ergonomics Consulting LLC — Proactive, metric-driven ergonomics. Remote office evaluations, PDAs, and program development."/>
  <!-- Load Inter font family and Tailwind CSS for utility classes (though mostly custom CSS is used here) -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap" rel="stylesheet">
  
  <style>
    /* =========================
       Brand Colors (edit here)
       ========================= */
    :root{
      --brand-maroon: #860038; /* Main Accent */
      --brand-black: #000000;  /* Backgrounds, text */
      --brand-white: #ffffff;  /* Minimal use — highlights and negative space */
      --muted: rgba(255,255,255,0.78);
      --muted-2: rgba(255,255,255,0.55);
      --bg: var(--brand-black);
      --card: rgba(255,255,255,0.03); /* Subtle dark card background */
      --radius:12px;
      --max-width:1100px;
      font-family: 'Inter', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }
    
    /* ====== Basic reset ====== */
    *{box-sizing:border-box}
    html,body{height:100%; margin:0}
    body{
      background: linear-gradient(180deg, var(--bg), #050505 140%);
      color:var(--muted);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
      padding-bottom:60px;
    }

    .container{max-width:var(--max-width); margin:0 auto; padding:28px;}

    /* ===== Layout & Components ===== */
    
    /* Header / Nav */
    header{
        display:flex;align-items:center;justify-content:space-between;
        gap:16px; position:sticky; top:0; z-index:50; 
        background:linear-gradient(180deg, rgba(0,0,0,0.45), rgba(0,0,0,0.25)); 
        padding:10px 0; border-bottom:1px solid rgba(255,255,255,0.05);
    }
    
    .brand{
        display:flex; align-items:center; gap:14px; text-decoration:none;
        color:inherit
    }
    
    /* Logo styling */
    .logo-mark img {
        height: 48px; 
        width: auto;
    }

    nav{display:flex;align-items:center;gap:12px}
    .nav-links{display:flex; gap:10px; align-items:center}
    a.link{color:var(--muted-2); text-decoration:none; padding:6px 8px; border-radius:8px; font-weight:600}
    a.link:hover{color:var(--brand-maroon); transition: color 0.2s;}
    
    /* Primary Call to Action Button */
    .cta{
        background:var(--brand-maroon); 
        color:var(--brand-white);
        padding:10px 16px; 
        border-radius:10px; 
        text-decoration:none; 
        font-weight:700;
        transition: background-color 0.2s, transform 0.1s;
    }
    .cta:hover {
        background: #a0104e; /* Slightly lighter maroon */
        transform: translateY(-1px);
    }

    /* Hero */
    .hero{display:grid; grid-template-columns:1fr 420px; gap:22px;
    align-items:center; margin-top:18px}
    @media (max-width:980px){ 
        .hero{grid-template-columns:1fr} 
        .nav-links{display:none} 
    }
    h1{color:var(--brand-white); font-size:clamp(1.8rem,3.2vw,2.5rem);
    margin:0 0 12px}
    p.lead{color:var(--muted); margin:0 0 12px; font-size:1.05rem;}

    .card{
        background:var(--card); 
        border-radius:var(--radius); 
        padding:20px;
        border:1px solid rgba(255,255,255,0.05);
    }
    .small{font-size:0.9rem; color:var(--muted-2)}
    .cta-row{display:flex; gap:10px; margin-top:16px; align-items:center}

    /* Services & features */
    .features{display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-top:18px}
    @media (max-width:980px){ .features{grid-template-columns:repeat(2,1fr)} }
    @media (max-width:560px){ .features{grid-template-columns:1fr} }
    .feature h3{margin:0 0 8px; color:var(--brand-white)}

    /* About Section Specific Styles */
    .expertise-list {
      list-style: none;
      padding: 0;
      margin-top: 10px;
      display: flex;
      flex-wrap: wrap;
      gap: 10px 20px;
    }
    .expertise-list li {
      color: var(--brand-maroon);
      font-weight: 600;
      font-size: 0.95rem;
    }
    .expertise-list li::before {
      content: "»"; 
      margin-right: 8px;
      color: var(--muted-2);
    }
    
    /* Process (Remote Eval Detail) & Contact */
    .process{display:grid; grid-template-columns:1fr 340px; gap:22px; margin-top:18px}
    @media (max-width:980px){ .process{grid-template-columns:1fr} }

    /* Form */
    label{display:block; font-size:0.9rem; margin-bottom:6px; color:var(--muted-2)}
    input,textarea,select{
      width:100%; padding:12px; border-radius:10px; border:1px solid rgba(255,255,255,0.06); 
      background:rgba(255,255,255,0.02); color:var(--muted); font-size:0.95rem;
    }
    button.primary{
      background:var(--brand-maroon); 
      color:var(--brand-white); 
      border:0; 
      padding:12px 18px; 
      border-radius:10px; 
      font-weight:700; 
      cursor:pointer;
      transition: background-color 0.2s, transform 0.1s;
    }
     button.primary:hover {
        background: #a0104e; /* Slightly lighter maroon */
        transform: translateY(-1px);
    }

    footer{margin-top:40px; text-align:center; color:var(--muted-2);
    font-size:0.9rem; padding-bottom:18px}
    
    /* helper */
    .tag{display:inline-block;padding:6px 12px;border-radius:999px;background:rgba(134,0,56,0.1); color:var(--brand-maroon); font-weight:700; border: 1px solid var(--brand-maroon);}
    .muted{color:var(--muted-2)}

    /* subtle focus styles */
    a,button,input,textarea{outline-color: rgba(134,0,56,0.5)}
  </style>
</head>
<body>
  <header class="container" role="banner">
    <a class="brand" href="#top" aria-label="Home">
      <!-- Logo Mark (Image File) - Ensure 'Ergo LLC Logo - MAIN.png' is in the same folder! -->
      <div class="logo-wrap" style="align-items:flex-start;">
        <div class="logo-mark" role="img" aria-label="Ergonomics Consulting logo">
          <img src="Ergo LLC Logo - MAIN.png" alt="Ergonomics Consulting Logo: Stylized spine curve and company name" />
        </div>
      </div>
    </a>

    <nav aria-label="Main navigation">
      <div class="nav-links" role="navigation" aria-hidden="false">
        <a class="link" href="#services">Services</a>
        <a class="link" href="#about">About</a>
        <a class="link" href="#process">Remote Evals</a>
      </div>

      <a class="cta" href="#contact">Book Eval</a>
    </nav>
  </header>

  <main class="container" id="top" role="main">
    
    <!-- HERO -->
    <section class="hero" aria-labelledby="hero-heading">
      <div>
        <h1 id="hero-heading">Proactive Ergonomics that Measurably Reduce Risk and Cost</h1>
        <p class="lead">Ergonomics Consulting LLC helps organizations and individuals prevent musculoskeletal injuries through **metric-driven assessment**, defensible job analyses, and practical solutions — delivered in-person or remotely.</p>

        <div class="card" style="margin-top:18px">
          <div style="display:flex;flex-direction:column;gap:10px">
            <div style="display:flex;gap:10px;align-items:center;">
              <span class="tag">Remote Evaluations</span>
              <span class="tag">PDA / POET</span>
              <span class="tag">Proactive Programs</span>
            </div>
            <div class="small">We specialize in remote office assessments, providing a clear **risk score** and a follow-up check to demonstrate measurable improvement.</div>

            <div class="cta-row">
              <a class="cta" href="#process">See Remote Packages</a>
              <a class="link" href="#contact">Contact for Industrial Services</a>
            </div>
          </div>
        </div>
      </div>

      <!-- Quick info card -->
      <aside class="card" aria-label="Quick details">
        <h3 style="margin:0 0 8px; color:var(--brand-white)">Start Remote Today</h3>
        <p class="muted" style="margin:0 0 10px">Book a 30- or 60-minute remote evaluation. Get immediate, practical coaching plus a summary report and a quantifiable risk score.</p>
        <div class="muted small" style="margin-top:14px; font-size:0.88rem; border-top:1px solid rgba(255,255,255,0.05); padding-top:14px;">
          <strong>COI Compliance:</strong> All services are strictly separate from any affiliated university role, adhering to all institutional Conflict of Interest requirements.
        </div>
      </aside>
    </section>

    <!-- SERVICES -->
    <section id="services" aria-labelledby="services-heading" style="margin-top:40px">
      <h2 id="services-heading" style="color:var(--brand-white); margin:0 0 8px">Core Services</h2>

      <div class="features" role="list">
        <article class="feature card" role="listitem">
          <h3>Remote Office Ergonomics</h3>
          <p class="muted">Video-based assessment for home & hybrid workers. Immediate adjustment coaching, an evidence-based **risk score**, and a prioritized action list with specific product recommendations.</p>
          <div style="margin-top:10px"><strong>Packages:</strong> QuickFix (30 min), Deep Dive (60 min), Team Pilot (bulk pricing).</div>
        </article>

        <article class="feature card" role="listitem">
          <h3>Physical Demands Analysis (PDA / POET)</h3>
          <p class="muted">Objective, defensible job analyses suitable for hiring screens, ADA accommodations, and labor compliance. Deliverables include standardized job demands, essential functions, and defensible PDAs.</p>
        </article>

        <article class="feature card" role="listitem">
          <h3>Program Design & Risk Reduction</h3>
          <p class="muted">Create sustainable ergonomics programs: risk scoring frameworks, training, and measurement systems that show tangible reductions in exposure and claims in industrial and office settings.</p>
        </article>
      </div>
    </section>

    <!-- ABOUT / AUTHORITY (New Section) -->
    <section id="about" aria-labelledby="about-heading" style="margin-top:40px">
      <h2 id="about-heading" style="color:var(--brand-white); margin:0 0 8px">Our Proactive Philosophy & Expertise</h2>
      <div class="card" style="padding:22px;">
        <p class="lead" style="color:var(--brand-maroon); font-weight:700;">
          Safety is a leading indicator, not a lagging one.
        </p>
        <p class="muted">
          We founded this firm on the belief that workplace safety must be **proactively built**, moving beyond the costly cycle of simply reacting to injuries. Our core mission is **injury prevention** through metric-driven risk reduction [cite: user context].
        </p>
        
        <h3 style="color:var(--brand-white); margin-top:20px; margin-bottom:8px;">Core Expertise: Prevention & Analysis</h3>
        
        <ul class="expertise-list">
          <li>Ergonomics & Injury Prevention (Primary Role)</li>
          <li>Systematic Risk Scoring & Reduction</li>
          <li>Physical Demands Analysis (PDA)</li>
          <li>Post-Offer Employment Testing (POET)</li>
        </ul>

        <p class="muted" style="margin-top:20px;">
          We systematically identify risk factors and reduce exposure to the lowest level that is both technically and financially feasible [cite: user context]. We apply this approach across all work environments—office, industrial, and laboratory.
        </p>
      </div>
    </section>

    <!-- REMOTE EVAL DETAIL (Refined Process Section) -->
    <section id="process" 
    aria-labelledby="process-heading" style="margin-top:40px">
      <h2 id="process-heading" style="color:var(--brand-white); margin:0 0 8px">Remote Office Evaluations: How It Works Anywhere</h2>

      <div class="process">
        <div class="card">
          <h3 style="color:var(--brand-white); margin-top:0;">The Value of a Virtual Assessment</h3>
          <p class="muted" style="margin-bottom:12px;">
            Our virtual assessment is designed for **convenience and consistency**, delivering professional-grade ergonomic analysis to your home or satellite office, anywhere in the world.
          </p>

          <ol style="margin:0 0 0 18px; padding:0; color:var(--muted)">
            <li style="margin-bottom:12px;"><strong>Step 1: Book & Intake:</strong> Select your package (QuickFix or Deep Dive) and schedule via the contact form or external scheduler. You'll receive a short, automated self-assessment guide.</li>
            <li style="margin-bottom:12px;"><strong>Step 2: Live Evaluation:</strong> We connect via a 30–60 minute video call (Zoom, Teams, etc.). We observe your posture, coach immediate adjustments, and capture key measurements and risk factors.</li>
            <li style="margin-bottom:12px;"><strong>Step 3: Risk Score Report:</strong> You receive a summary with a **quantified risk score** (e.g., 8/10), a prioritized list of practical fixes, and specific product recommendations.</li>
            <li><strong>Step 4: Measurable Follow-up (Optional):</strong> We offer an optional 30-day check-in to confirm changes and calculate your **risk reduction percentage**, proving the efficacy of the solutions.</li>
          </ol>
        </div>

        <aside class="card">
          <h4 style="margin-top:0; color:var(--brand-maroon)">Why Our Remote Process Works</h4>
          <ul style="list-style:none; padding:0;">
            <li style="margin-bottom:12px;"><strong style="color:var(--brand-white);">Scalable:</strong> Ideal for pilot programs or distributed teams—we assess any location.</li>
            <li style="margin-bottom:12px;"><strong style="color:var(--brand-white);">Data-Driven:</strong> Focus on leading metrics (risk scores) instead of lagging ones (injury history).</li>
            <li style="margin-bottom:12px;"><strong style="color:var(--brand-white);">Cost-Effective:</strong> Avoids travel costs and reduces overhead while maintaining professionalism.</li>
            <li><strong style="color:var(--brand-white);">Immediate Action:</strong> Focus on coaching and small behavioral changes during the live session for instant relief.</li>
          </ul>

          <div style="margin-top:20px">
            <a class="cta" href="#contact">Book Your Deep Dive Now</a>
          </div>
        </aside>
      </div>
    </section>

    <!-- CONTACT / BOOKING -->
    <section id="contact" aria-labelledby="contact-heading" style="margin-top:40px">
      <h2 id="contact-heading" style="color:var(--brand-white); margin:0 0 8px">Contact & Book</h2>

      <div class="process">
        <form id="contactForm" class="card" onsubmit="return handleSubmit(event)" novalidate>
          <div style="display:flex;gap:10px;">
            <div style="flex:1">
              <label for="name">Name</label>
              <input id="name" name="name" type="text" required placeholder="Your name">
            </div>
            <div style="width:180px">
              <label for="phone">Phone</label>
              <input id="phone" name="phone" type="tel" placeholder="Optional">
            </div>
          </div>

          <div style="margin-top:12px">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" required placeholder="you@example.com">
          </div>

          <div style="margin-top:12px">
            <label for="service">Service interested in</label>
            <select id="service" name="service">
              <option>Remote Office Evaluation — QuickFix (30 min)</option>
              <option>Remote Office Evaluation — Deep Dive (60 min)</option>
              <option>Team Pilot / Program Consulting</option>
              <option>Physical Demands Analysis (PDA / POET)</option>
              <option>Other / Consultation</option>
            </select>
          </div>

          <div style="margin-top:12px">
            <label for="message">Message / Project details</label>
            <textarea id="message" name="message" rows="4" placeholder="Tell me about the role, team size, or specific concerns..."></textarea>
          </div>

          <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:16px">
            <button class="primary" type="submit">Send Inquiry</button>
          </div>
          <div id="formStatus" style="margin-top:12px;color:var(--muted); font-size:0.95rem"></div>
        </form>

        <!-- Booking / quick links -->
        <aside class="card">
          <h4 style="margin-top:0; color:var(--brand-white)">Quick Booking & Payment</h4>
          <p class="muted small">For instant scheduling and payment for remote services, use an integrated tool like Calendly or Square Booking.</p>

          <!-- Example placeholder button for Calendly (REPLACE href with your actual link) -->
          <div style="margin-top:12px">
            <a class="cta" href="https://calendly.com/your-ergonomics-link" target="_blank" rel="noopener">Schedule Remote Eval Now</a>
          </div>

          <div style="margin-top:20px; font-size:0.88rem; color:var(--muted-2); border-top:1px solid rgba(255,255,255,0.05); padding-top:20px;">
            <strong>For Industrial or Corporate Inquiries:</strong> Use the form to the left or call us directly to discuss custom program scoping.
          </div>
        </aside>
      </div>
    </section>

    <footer>
      <div style="max-width:820px;margin:0 auto">
        © <span id="year"></span> Ergonomics Consulting LLC • All rights reserved.
      <div style="margin-top:8px; font-size:0.85rem; color:var(--muted-2)">Built simple for easy, self-managed updates.</div>
      </div>
    </footer>
  </main>

  <script>
    // Fills the current year into the footer
    document.getElementById('year').textContent = new Date().getFullYear();

    // Basic client-side form handler (Placeholder - needs backend integration)
    function handleSubmit(e){
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const status = document.getElementById('formStatus');
      
      if(!name || !email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){
        status.textContent = 'Please enter a valid name and email address.';
        return false;
      }

      // ------------------------------------------------------------------
      // NOTE: This demo does not send mail. To receive messages, you MUST:
      // 1. Change the form's action attribute to a service like Formspree 
      //    (e.g., <form action="https://formspree.io/f/your-id" method="POST">).
      // 2. Or, connect this form to a backend script (like a cloud function).
      // ------------------------------------------------------------------

      status.textContent = 'Success! Your inquiry is ready. Connect this form to a backend service like Formspree or Netlify Forms to start receiving emails.';
      e.target.reset();
      return false;
    }
  </script>
</body>
</html>
